{
  "name": "node-slack-webhook",
  "version": "0.0.1",
  "description": "to post messages to Slack using its webhook integration",
  "main": "./lib/slack-webhook.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/danielmontenegro/node-slack-webhook.git"
  },
  "engines": ["node >=0.10.0"],
  "author": "Daniel Montenegro",
  "dependencies": {
    "minimist": "^1.2.0",
    "request": "^2.81.0"
  }
}
